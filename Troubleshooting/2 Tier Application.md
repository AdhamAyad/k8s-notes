2 Services
1 Deployment
2 pods


first check the pods and their env var
then check that the services eposes the correct ports and select the true pods
the inspect the deployment  to be able to edit the pods thats manage by deploment


or we can start with front end service then deployment then db service then db pod